const { ToastContainer } = require("react-toastify");

export const HeaderAdmin = () => {
    return (
        <div style={{height:'40px'}}><ToastContainer position="top-center"></ToastContainer></div>
    );
}